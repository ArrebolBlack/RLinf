# Archived RLD2-QA recipes

These Cycle-1/Cycle-2 configs are retained only to reproduce historical policy
search evidence. They are not CPU production defaults. New residual training uses
the `quality_v2_final_source_*.yaml` W32 recipes in the parent directory; whole-host
throughput runs explicitly use `cpu_residual_throughput_j32w2.yaml`.
